version = "1.0"

[task]
name = "ddd-architectural-challenges/java-order-dispatch-domain-entities"
description = "Move order dispatch logic from service layer into domain entities in a refactoring kata"

[metadata]
language = "Java"
framework = "plain Java"
source_repo = "https://github.com/gregorriegler/order-dispatch-refactoring-kata"
diversity_axes = [
    "language:java",
    "difficulty:easy",
    "pattern:encapsulation",
]
name = "java-order-dispatch-domain-entities"
description = "Move order dispatch logic from service layer into domain entities in a refactoring kata"
difficulty = "easy"

[agent]
timeout_sec = 1200

[environment]
memory_mb = 4096

[verifier]
timeout_sec = 300

[nasde.source]
git = "https://github.com/gregorriegler/order-dispatch-refactoring-kata.git"
ref = "master"
