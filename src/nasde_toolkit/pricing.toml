# Model pricing for token-cost metrics (per 1M tokens).
#
# Cost is CACHE-AWARE (ADR-014, supersedes ADR-011's full-rate formula):
#   cost = fresh_input * input_per_1m
#        + cache_writes * cache_write_per_1m
#        + cache_reads  * cached_input_per_1m
#        + output       * output_per_1m
# where fresh_input = total_prompt_tokens - cache_reads - cache_writes.
# A missing cache rate falls back to input_per_1m (conservative, never a silent
# discount). The cache-free ceiling is NOT stored — it is derivable from the
# recorded token_usage as input * input_per_1m + output * output_per_1m.
#
# cache_write_per_1m for Anthropic models is the 1-hour cache write rate
# (2x base input) — the mode Claude Code uses; verified against Harbor's own
# per-step cost accounting on the 2026-07 grid. OpenAI bills no write premium,
# so gpt entries omit the field (writes fall back to the base input rate).
#
# Cache reads are 0.1x the base input rate on every Claude model EXCEPT
# Claude Fable 5.1 (and Mythos 5.1), where they are 0.025x — $0.25/MTok on a
# $10/MTok input rate. That is a published per-model exception, not a typo.
#
# Claude models re-verified 2026-09-04 against the official pricing page (Fable 5,
# Opus 5, Opus 4.8, Sonnet 5, Sonnet 4.6 unchanged since 2026-08-24; Fable 5.1
# added) — CONFIRM before publishing any cost figures. gpt entries not re-checked
# since 2026-06-08.

[models."gpt-5.5"]
input_per_1m = 5.0
output_per_1m = 30.0
cached_input_per_1m = 0.50
as_of = "2026-06-08"
source = "https://developers.openai.com/api/docs/pricing"

[models."gpt-5.4"]
input_per_1m = 2.50
output_per_1m = 15.0
cached_input_per_1m = 0.25
as_of = "2026-06-08"
source = "https://developers.openai.com/api/docs/pricing"

[models."claude-opus-5"]
input_per_1m = 5.0
output_per_1m = 25.0
cached_input_per_1m = 0.50
cache_write_per_1m = 10.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"

[models."claude-opus-4-8"]
input_per_1m = 5.0
output_per_1m = 25.0
cached_input_per_1m = 0.50
cache_write_per_1m = 10.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"

[models."claude-fable-5-1"]
input_per_1m = 10.0
output_per_1m = 50.0
cached_input_per_1m = 0.25
cache_write_per_1m = 20.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"

[models."claude-fable-5"]
input_per_1m = 10.0
output_per_1m = 50.0
cached_input_per_1m = 1.0
cache_write_per_1m = 20.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"

[models."claude-sonnet-5"]
input_per_1m = 2.0
output_per_1m = 10.0
cached_input_per_1m = 0.20
cache_write_per_1m = 4.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"

[models."claude-sonnet-4-6"]
input_per_1m = 3.0
output_per_1m = 15.0
cached_input_per_1m = 0.30
cache_write_per_1m = 6.0
as_of = "2026-09-04"
source = "https://platform.claude.com/docs/en/about-claude/pricing"
